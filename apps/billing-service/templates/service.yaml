apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.name }}
    app.kubernetes.io/part-of: billings
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.name }}
  ports:
    - port: 80
      targetPort: {{ .Values.port }}
